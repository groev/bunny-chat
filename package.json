{
  "name": "bunny-chat",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/client && npm run build --workspace=apps/server"
  }
}
